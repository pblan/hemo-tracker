[package]
name = "hemo-source-file-encryption-proof"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hemo-source-file-encryption = { path = "../../crates/source-file-encryption", features = ["proofs"] }

[dev-dependencies]
tempfile = "=3.22.0"
