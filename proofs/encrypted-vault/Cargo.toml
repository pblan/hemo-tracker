[package]
name = "hemo-encrypted-vault-proof"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hemo-encrypted-vault = { path = "../../crates/encrypted-vault", features = ["proofs"] }

[dev-dependencies]
tempfile = "=3.22.0"
